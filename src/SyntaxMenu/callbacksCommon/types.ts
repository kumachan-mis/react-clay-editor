export interface ContentConfig {
  facingMeta: string;
  content: string;
  trailingMeta: string;
  nestedSearch?: boolean;
}

export interface ContentMetaConfig {
  facingMeta: string;
  trailingMeta: string;
  nestedSearch?: boolean;
}
