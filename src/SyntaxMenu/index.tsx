import * as React from 'react';

import { IconButton } from './components';
import { Bold as BoldIcon } from './icons';

export const SyntaxMenu: React.FC = () => (
  <IconButton>
    <BoldIcon />
  </IconButton>
);
