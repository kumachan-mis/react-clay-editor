<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="author" content="Yuya Suwa">
    <title>Playwright Test</title>
    <meta name="description" content="A text document editor which is syntactically formattable in real time.">
  </head>
  <body>
    <div id="app"></div>
    <script crossorigin src="https://unpkg.com/react@<%= reactVersion %>/umd/react.<%= mode == 'production' ? 'production.min' : 'development' %>.js"></script>
    <script crossorigin src="https://unpkg.com/react-dom@<%= reactVersion %>/umd/react-dom.<%= mode == 'production' ? 'production.min' : 'development' %>.js"></script>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/katex@<%= katexVersion %>/dist/katex<%= mode == 'production' ? '.min' : '' %>.css" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/katex@<%= katexVersion %>/dist/katex<%= mode == 'production' ? '.min' : '' %>.js" crossorigin="anonymous"></script>
  </body>
</html>

<style>
  @media screen {
    html, body {
      width: 100%;
      height: 100%;
      margin: 0;
      padding: 0;
    }

    #app {
      position: absolute;
      width: 100%;
      height: 100%;
      margin: 0;
      padding: 0;
    }
  }
</style>
  