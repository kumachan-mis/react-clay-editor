import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="API/EditorTextFieldRoot" />

# EditorTextFieldRoot

The container of scrollable text fields. This grows taller until it fills up the space of the editor.

import Api from '../ts/Api';

<div style={{ display: 'flex', justifyContent: 'center' }}>
  <Api colorized="scrollable-container" />
</div>

## Import

```ts
import { EditorTextFieldRoot } from 'react-clay-editor';
```

## Props

| name     | type        | requried | description              |
| -------- | ----------- | -------- | ------------------------ |
| children | `ReactNode` | no       | content of the component |
